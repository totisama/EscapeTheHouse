using UnityEngine;

public static class Globals
{
    public static Bounds RoomBounds;

    public enum ItemTypes
    {
        key,
        finalKey,
        book,
        machine
    }
}
